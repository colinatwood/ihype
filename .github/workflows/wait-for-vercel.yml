# Vercel workflow removed — project now deploys via Cloudflare Pages.
# This file is kept as a placeholder so existing branch protection rules
# that reference "Wait for Vercel preview" don't break immediately.
name: Vercel Deployment Ready (deprecated)
on:
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  skip:
    name: Wait for Vercel preview
    runs-on: ubuntu-latest
    steps:
      - run: echo "Cloudflare Pages is now used for deployments. This check is a no-op."
